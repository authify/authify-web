          <% @identities.each do |ident| %>
          <tr>
            <td><%= icon ident.attributes[:provider] %> <%= ident.attributes[:provider] %></td>
            <td><%= ident.attributes[:uid] %></td>
            <td>
              &nbsp;
            </td>
          </tr>
          <% end %>
